
## If using AWS Bedrock.

Use `export AWS_PROFILE=<your profile here>` to set an AWS profile.

Use `aws sso login` to sso to the profile.

Ensure you're using the `ChatBedrockConverse` langchain object.
